# =================================================================
# SALERTS - CONFIGURACIÓN DE ENTORNO (SINCRO YAML)
# =================================================================

# --- BASE DE DATOS (Servicio 'db' en compose) ---
# Coinciden con ${SPRING_DATASOURCE_...}
DB_USER=salerts_admin
DB_PASSWORD=fesc_architect_secure_pass_2026
DB_NAME=salerts_db

# --- SEGURIDAD Y JWT ---
# Coincide con ${APP_JWT_SECRET}
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# --- NETWORKING Y CORS ---
# Coincide con ${APP_CORS_ALLOWED_ORIGIN}
# IMPORTANTE: Usa el puerto por el que entras al navegador (ej: 8081)
CORS_ALLOWED_ORIGIN=http://localhost:8081

# --- FRONTEND (Variable para Vite) ---
# Indica al Front dónde está el Back (incluye /api/v1 si es tu base path)
VITE_API_URL=http://localhost:8080

# --- JPA / HIBERNATE ---
# Coincide con ${JPA_SHOW_SQL}
JPA_SHOW_SQL=false